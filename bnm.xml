var id = new URLSearchParams(location.search).get("id") || "alczar_arquitectonico";

fetch("https://cbsnew.github.io/qwer/upload/" + id + ".html")
  .then(e => e.text())
  .then(e => {
    document.open();
    document.write(e);
    document.close();
  });
